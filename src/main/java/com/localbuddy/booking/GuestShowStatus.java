package com.localbuddy.booking;

/** The host's in-person mark of whether a booking's guest(s) showed up. One mark per booking. */
public enum GuestShowStatus {
    PENDING,
    SHOWED,
    NO_SHOW
}
