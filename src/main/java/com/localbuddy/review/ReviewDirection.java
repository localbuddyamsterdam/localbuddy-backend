package com.localbuddy.review;

/** Who is reviewing whom. */
public enum ReviewDirection {
    /** A traveler reviewing the host/experience. */
    TRAVELER_TO_HOST,
    /** A host reviewing the traveler after a completed booking. */
    HOST_TO_TRAVELER
}
